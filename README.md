# Bake & Party - מערכת ניהול חנות ואתר אינטרנט

ברוכים הבאים לפרויקט החנות של **Bake & Party** (אפייה ומסיבות)! פרויקט זה כולל מסד נתונים מקומי, שרת ניהול חכם וממשק משתמש מותאם אישית.

---

## 🛠️ טכנולוגיות בשימוש
* **מסד נתונים:** PostgreSQL (רץ דרך Docker)
* **צד שרת (Backend):** FastAPI (Python)
* **צד לקוח (Frontend):** HTML5, CSS3, JavaScript
* **ניהול גרסאות:** Git & GitHub

---

## 🚀 הוראות הפעלה (מה צריך להריץ בכל פעם שמתחילים לעבוד)

### 1. הפעלת מסד הנתונים (Docker)
פתחי טרמינל בתיקיית הפרויקט והריצי:
```bash
docker compose up -d
```
זה מפעיל את ה-PostgreSQL (פורט 5433) ואת pgAdmin (פורט 5050, אימייל admin@admin.com / סיסמה adminpassword) לצפייה נוחה בטבלאות אם תרצי.

### 2. התקנת חבילות פייתון (פעם ראשונה בלבד, או אחרי עדכון requirements.txt)
```bash
pip install -r requirements.txt
```

### 3. יצירת הטבלאות הבסיסיות ומשתמש מנהל
```bash
python create_admin.py
```
הסקריפט הזה יוצר את טבלאות `users`, `settings`, `weekly_hours` (שעות שבועיות עם ברירת מחדל 09:00–18:00 לכל יום) ו-`special_days` (ימים מיוחדים/חגים), ומבקש ממך להקים משתמש מנהל. **חשוב להריץ את זה פעם אחת לפני ההרצה הראשונה של השרת** — בלעדיו כל מה שקשור לשעות פתיחה יקרוס.

### 4. ייבוא המוצרים מהאקסל של וולט
ודאי שקובץ ה-xlsx (ההורדה מוולט) נמצא בתיקיית הפרויקט, ואז:
```bash
python import_data.py
```
הסקריפט מזהה אוטומטית את קובץ ה-xlsx היחיד בתיקייה. **שימי לב: הרצה חוזרת מוחקת ובונה מחדש את טבלאות Products ו-Categories** (כדי לרענן מוולט), אבל לא נוגעת במשתמשים/הגדרות/שעות. אם יש כמה קבצי xlsx בתיקייה, ציינו איזה: `python import_data.py "שם_הקובץ.xlsx"`.

### 5. הפעלת השרת (Backend)
```bash
uvicorn main:app --reload
```
השרת יעלה על `http://localhost:8000`.

### 6. פתיחת האתר
פשוט פתחי את `index.html` בדפדפן. לניהול (שעות, משלוחים, קטגוריות מוצרים) — `admin.html`.

---

## 🏷️ ניהול קטגוריות למוצרים עם שיוך כפול
ב-`admin.html` יש טאב חדש **"מוצרים וקטגוריות"**: רשימה של כל המוצרים בחנות, עם אפשרות לבחור לכל מוצר קטגוריה ראשונה ושנייה מרשימה נפתחת, ולשמור בלחיצה אחת. מוצרים שאין להם אף קטגוריה מסומנים באדום עם אזהרה — **מוצר כזה לא מוצג באתר הראשי בכלל**, אז שווה לעבור על הפילטר "ללא קטגוריה" פעם אחת אחרי כל ייבוא מוולט ולוודא שלכולם יש שיוך.

---

## ⚠️ באגים שתוקנו (25.08.2026)

1. **טבלאות שעות פעילות לא היו קיימות בכלל.** `main.py` השתמש בטבלאות `weekly_hours` ו-`special_days`, אבל שום סקריפט לא יצר אותן בפועל — מה שגרם לכל מה שקשור לשעות פתיחה (סטטוס פתוח/סגור, לוח שעות באתר, פאנל השעות בניהול) לקרוס. תוקן: `create_admin.py` יוצר וזורע אותן עם ברירת מחדל, ו-`main.py` כולל רשת ביטחון נוספת בעליית השרת.
2. **שם קובץ האקסל הקבוע ב-`import_data.py`** לא תאם את שם הקובץ שיורד בפועל. תוקן: הסקריפט מזהה עכשיו אוטומטית את קובץ ה-xlsx בתיקייה (או מקבל שם כפרמטר).
3. **אייקון הוואטסאפ** היה אימוג'י גנרי (🟢) — הוחלף בסמל הוואטסאפ האמיתי (SVG).
4. **טאב שיוך קטגוריות כפול** נוסף ל-`admin.html` כמבוקש.

---

## ✅ רשימת בדיקה לפני שהאתר עולה לאוויר (חשוב מאוד!)

- [ ] **כתובת ה-API היא `http://localhost:8000` בקוד** (ב-`index.html`, ב-`admin.html`, ובברירת המחדל של `main.py`). זה עובד רק על המחשב שלך. לפני שהאתר יעלה לשרת אמיתי, **חובה** לעדכן את זה לכתובת הדומיין/שרת הציבורי שלך בכל שלושת הקבצים (`const API = ...`).
- [ ] **הסיסמאות של מסד הנתונים כתובות ישירות בקוד** (`admin` / `adminpassword`) בכמה קבצים. לפני שמעלים את הריפו ל-GitHub (גם אם פרטי), עדיף להעביר אותן למשתני סביבה (קובץ `.env`, שכבר מוגדר ב-`.gitignore` ולא יעלה לגיט) במקום להשאיר אותן בקוד עצמו.
- [ ] **CORS פתוח לכולם** (`allow_origins=["*"]`) — טוב לפיתוח, אבל כשהאתר עולה לאוויר כדאי להגביל לכתובת האתר האמיתית בלבד, מטעמי אבטחה.
- [ ] **חיבורי משתמשים (sessions) נשמרים בזיכרון של השרת** — כלומר אם השרת מתאתחל/קורס, כולם (כולל את) יצטרכו להתחבר שוב. מספיק לחנות קטנה, אבל שווה לדעת.
- [ ] **גיבוי למסד הנתונים** — לפני שהאתר "רשמי", כדאי לוודא שיש תהליך גיבוי כלשהו ל-PostgreSQL (גם export ידני מפעם לפעם עדיף על כלום).
- [ ] בדקי את האתר ממכשיר נייד אמיתי, לא רק מהדפדפן במחשב.
- [ ] עברי על טאב "מוצרים וקטגוריות" ב-admin.html פעם אחת ווודאי שאין מוצרים "ללא קטגוריה".
- [ ] תזכרי: **אין כרגע מערכת הזמנות** — האתר מציג מוצרים, שעות פתיחה, ומידע ליצירת קשר בלבד. יש הודעה סטטית על משלוחים מעל 200 ₪ שמחולקים אחרי שעות הסגירה — כדאי לוודא שהניסוח הזה עדיין נכון לפני ההשקה.

